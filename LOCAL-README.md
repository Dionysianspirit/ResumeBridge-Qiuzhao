# 履历桥 · 秋招助手（本地加载）

版本：0.2.0-local.1。面向 Chrome / Edge，通过开发者模式加载。

基于 [xiaocheng223/ResumeBridge](https://github.com/xiaocheng223/ResumeBridge)，保留原 MIT 许可与署名。
这是社区公开版，未发布到浏览器扩展商店。

## 安装

1. 打开 `chrome://extensions/` 或 `edge://extensions/`。
2. 开启右上角「开发者模式」，点击「加载已解压的扩展程序」。
3. 选择本仓库根目录（包含 `manifest.json` 的文件夹）。
4. 将「ResumeBridge 履历桥 - 秋招助手」固定到工具栏。

加载扩展时请选择解压后的文件夹，并在使用期间保留该文件夹。

## 投递总表

点击插件弹窗顶部的「打开投递总表」，即可在独立标签页查看。
无需启动服务器。

- 快照日期：2026-09-06。
- 有效来源约 2,812 行，合并为约 2,788 条；本周优先 100 条。
- 过期来源单独放在「已过期」视图。
- 公开版已去掉「挂钩提示」和「软岗分」；公司、岗位、链接、档位、截止日期等招聘信息保留。
- 默认用内置总表。也可以在总表页导入自己的 `catalog.json`，或点「恢复内置总表」换回来。

「未过期」按原表日期计算，不表示已经逐站核实官网仍在开放。
明确日期每天重新计算；「招满为止」等滚动截止不编造日期。

## 第一次使用

1. 点击插件图标 → 设置，录入资料后保存。
2. 需要不同岗位版本时，可另存为新简历，最多 20 份。
3. 在真实网申表单页打开插件，确认本次使用的简历，再点「开始填写」。
4. 核对绿色已填字段，补充橙色待处理字段。附件、验证码与最终提交由你操作。
5. 提交后再记录投递；总表会同步进度。

`sample-profile.json` 全是虚构测试数据，不会自动写入真实资料。
